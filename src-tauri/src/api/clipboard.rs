// use tauri::AppHandle;

// pub fn start_monitor(_app_handle: AppHandle) -> Result<(), Box<dyn std::error::Error>> {
//     // This function is currently not needed since we're using the clipboard plugin
//     Ok(())
// }
